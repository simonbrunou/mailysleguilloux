#!/usr/bin/env bash
# Deprecated. Images are now hosted in the R2 bucket `mailysleguilloux-images`
# and served by the Worker at /images/*. To (re-)upload, use:
#
#   ./scripts/upload-images.sh
#
# This script is kept only as a stub so existing docs/links don't 404.

echo "download-images.sh is deprecated — use scripts/upload-images.sh instead." >&2
exit 1
